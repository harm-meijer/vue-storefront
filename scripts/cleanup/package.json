{
  "name": "cleanup",
  "version": "1.0.0",
  "description": "Clean up everything when build fails",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Harm Meijer",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^9.0.1",
    "glob": "^7.1.6"
  }
}
